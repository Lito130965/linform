# Exact Python dependency versions, the counterpart to frontend/package-lock.json.
#
# pyproject.toml states what the project NEEDS (">=", the compatibility floors);
# this file states what it was BUILT AND TESTED WITH. Without it every install
# resolves to whatever PyPI happened to publish that morning, which showed up in
# three ways worth naming:
#
#   - the API contract moved on its own. FastAPI 0.141 spells a binary upload
#     body "contentMediaType" where 0.129 spelled it "format": "binary" — a real
#     difference for anyone generating a client, arriving in a pull request that
#     touched neither the routers nor the schemas;
#   - the renderer is a dependency. Golden-PDF tests compare rendered output, so
#     a WeasyPrint (or pydyf, or fonttools) release could change page layout with
#     no commit behind it;
#   - the image was not reproducible. Base images are pinned by digest precisely
#     so that a build that passed CI is the build that ships — and then the
#     Python packages inside it floated anyway.
#
# Used as a CONSTRAINTS file, not a requirements file: it caps versions of
# whatever gets installed and never installs anything by itself, so a
# platform-specific dependency that is absent here (uvloop and uvicorn's other
# Linux-only extras) still installs normally.
#
#   pip install -e ".[dev]" -c constraints.txt     # development and CI
#   pip install . -c constraints.txt               # the image
#
# To bump: change the pins, run the suite and `python -m scripts.openapi_snapshot
# --check`, and commit the result together. A bump that alters the published API
# or a golden PDF is then visible in the same diff as the bump itself, which is
# the entire point.
#
# Generated from a clean resolution on 2026-08-02.

aiosqlite==0.22.1
alembic==1.18.5
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
brotli==1.2.0
certifi==2026.7.22
cffi==2.1.0
click==8.4.2
colorama==0.4.6
cssselect2==0.9.0
fastapi==0.141.1
fonttools==4.63.0
greenlet==3.5.4
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
Jinja2==3.1.6
Mako==1.3.12
MarkupSafe==3.0.3
packaging==26.2
pillow==12.3.0
pluggy==1.6.0
prometheus_client==0.26.0
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
pydyf==0.12.1
Pygments==2.20.0
pypdf==6.14.2
pyphen==0.17.2
pytest==9.1.1
python-barcode==0.16.1
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
segno==1.6.6
SQLAlchemy==2.0.51
starlette==1.3.1
tinycss2==1.5.1
tinyhtml5==2.1.0
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.52.1
watchfiles==1.2.0
weasyprint==69.0
webencodings==0.5.1
websockets==17.0.1
zopfli==0.4.3
